// Example JavaScript code to demonstrate interaction

// Example: Change content on button click
document.querySelectorAll('button').forEach(button => {
    button.addEventListener('click', () => {
        alert("Button clicked!");
    });
});
